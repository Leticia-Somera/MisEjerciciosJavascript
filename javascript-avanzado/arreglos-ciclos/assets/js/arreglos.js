/*Una forma de declarar arreglos es
const numeros = [5];
*/

let numeros = new Array();

numeros = [1,2,3,13,23,532,328,222,345,666];

for(let i = 0; i < numeros.length; i++) {
    document.write(`${numeros[i]} <br>`);    
 }